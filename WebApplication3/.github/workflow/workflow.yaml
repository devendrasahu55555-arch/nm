name: PipelineWorkflow

on:
  push:
    branches:
      - main
  pull_request:
    branches: 
      - main

jobs:
    build:
        name: Azure Pipeline
        runs-on: ubuntu-22.04
        steps:
        - name: Azure Pipelines Action
          uses: Azure/pipelines@v1
          with:
            azure-devops-project-url: https://dev.azure.com/devendradevops5/devprooject
            azure-pipeline-name: 'devendrasahu55555-arch.nm'
            azure-devops-token: ${{ secrets.AZURE_DEVOPS_TOKEN }}